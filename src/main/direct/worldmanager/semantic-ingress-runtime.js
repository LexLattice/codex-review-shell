"use strict";

// Compatibility import path for the WorldManager semantic-ingress service.
// The active implementation is semantic discharge: native action selection
// with structurally typed, semantically open arguments.
module.exports = require("./semantic-discharge-runtime");
