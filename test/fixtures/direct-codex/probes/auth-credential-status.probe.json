{
  "schema": "direct_codex_probe_manifest@1",
  "id": "probe.auth.credential_status",
  "name": "OAuth Credential Status Projection",
  "hypothesis": "Stored credential metadata projects to renderer-safe auth status with token presence flags but no raw tokens.",
  "fixture": {
    "source": "auth-shape-fixture",
    "authFixtureId": "auth/credential-status"
  },
  "authShape": {
    "operation": "credential_status_projection"
  },
  "acceptance": {
    "expectedState": "candidate",
    "requiredOutputs": [
      "status",
      "accountId",
      "hasAccessToken",
      "hasRefreshToken",
      "rawTokensExposed"
    ]
  },
  "blockedLiveGates": [
    "No credential file write.",
    "No renderer token exposure."
  ]
}
