{
  "schema": "direct_codex_probe_manifest@1",
  "id": "probe.auth.callback_error",
  "name": "OAuth Callback Error Shape",
  "hypothesis": "A synthetic OAuth callback error normalizes without code exchange, token mutation, or retry.",
  "fixture": {
    "source": "auth-shape-fixture",
    "authFixtureId": "auth/callback-error"
  },
  "authShape": {
    "operation": "callback_parse"
  },
  "acceptance": {
    "expectedState": "candidate",
    "requiredOutputs": [
      "status",
      "state",
      "error",
      "errorDescription"
    ]
  },
  "blockedLiveGates": [
    "No localhost callback server.",
    "No authorization code exchange.",
    "No token storage."
  ]
}
